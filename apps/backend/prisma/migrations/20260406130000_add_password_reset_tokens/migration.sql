-- This migration directory was missing from the repository but is referenced by
-- the Prisma migrations table. Keeping a no-op migration file here restores a
-- consistent local migration history so future migrations can run.

-- No-op
SELECT 1;

