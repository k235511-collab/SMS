/**
 * Module-specific business logic.
 *
 * Each module directory (students/, teachers/, etc.) will contain:
 * - API service functions (e.g. students.service.ts)
 * - Module-specific types
 * - Module-specific hooks
 *
 * This keeps feature logic decoupled from UI components.
 */
